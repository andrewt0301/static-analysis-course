package org.example.lexer;

public enum WhileTokens {
    EOF,

    COMMA,
    SEMI,
    LPARENT,
    RPARENT,

    PLUS,
    MINUS,
    DIV,
    MUL,
    REM,

    NOT,
    AND,
    OR,
    XOR,

    ID,
    NUM
}
